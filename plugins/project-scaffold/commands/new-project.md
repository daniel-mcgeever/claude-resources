---
description: Scaffold a new development project (Forgejo repo + .claude/ from catalogue + dev-hub registration)
---

Invoke the `new-project` skill to scaffold a new project on vm-160. Gather the
slug, description, tech stack, and component selections, then run the scaffold
end to end (Forgejo repo, files, git push, optional prod stack, dev-hub register).
